remaining work:
•look at all the comments starting with “todo”
•the first set of slides from the TA mentioned considerations of big and small endian, look into that
•correctly handle a file that does not exist
•if a file is not specified, return “/index.html”
•MAKE SURE it has the s/ash->->->->-^
•MULTIPLE CONNECTIONS
•fix the HTTP/1.1\n issue
•make sure to comment-in freeRequest(request)

